{
  "name": "app-flow",
  "version": "0.1.0",
  "private": true,
  "description": "Turn a Maestro test flow into a self-contained HTML canvas of an app's screens.",
  "type": "module",
  "scripts": {
    "build": "node src/build.mjs flows/placeholder.yaml dist/flow-map.html",
    "build:campus-connect": "node src/build.mjs flows/campus-connect.yaml dist/flow-map.html",
    "flow": "maestro test flows/campus-connect.yaml",
    "all": "npm run build",
    "all:campus-connect": "npm run flow && npm run build:campus-connect"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
